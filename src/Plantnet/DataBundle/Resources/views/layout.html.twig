<!DOCTYPE html">
<!--[if lt IE 7 ]> <html class="ie6"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8"> <![endif]-->
<!--[if IE 9 ]>    <html class="ie9"> <![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html> <!--<![endif]-->
<head>
	{% block head %}
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
		<!--[if IE]><![endif]-->
		<!-- Always force latest IE rendering engine (even in intranet) & Chrome Frame
		Remove this if you use the .htaccess -->
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
		<meta name="description" content=""/>
		<meta name="author" content="Barbe Julien (AMAP)"/>
		<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
		<!--[if lt IE 9]>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
		<![endif]-->
		<title>{% block title %}Plantnet{% endblock %}</title>
		<link type="image/x-icon" rel="shortcut icon" href="{{ asset('bundles/plantnetdata/images/favicon.ico') }}" />
		{% block stylesheets %}
			{% stylesheets filter='less,cssrewrite,?yui_css' output='css/publish.css' debug=false 'bundles/plantnetdata/less/bootstrap.less' %}
				<link href="{{ asset_url }}" type="text/css" rel="stylesheet" />
			{% endstylesheets %}
		{% endblock %}
		<style type="text/css">
			/* Override some defaults */
			html, body {
				background-color: #eee;
			}
			body {
				padding-top: 40px; /* 40px to make the container go all the way to the bottom of the topbar */
			}
			.container > footer p {
				text-align: center; /* center align it with the container */
			}
			.container {
				width: 70%; /* downsize our container to make the content feel a bit tighter and more cohesive. NOTE: this removes two full columns from the grid, meaning you only go to 14 columns and not 16. */
			}
			/* The white background content wrapper */
			.container > .content {
				background-color: #fff;
				padding: 20px;
				margin: 0 -20px; /* negative indent the amount of the padding to maintain the grid system */
				-webkit-border-radius: 0 0 6px 6px;
				-moz-border-radius: 0 0 6px 6px;
				border-radius: 0 0 6px 6px;
				-webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
				-moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
				box-shadow: 0 1px 2px rgba(0,0,0,.15);
			}
			/* Page header tweaks */
			.page-header {
				background-color: #f5f5f5;
				padding: 4px 8px 23px;
				margin: -20px -20px 20px;
			}
			/* Styles you shouldn't keep as they are for displaying this base example only */
			.content .span10,
			.content .span4 {
				min-height: 500px;
			}
			.content .span10{
				width: 76%;
			}
			/* Give a quick and non-cross-browser friendly divider */
			.content .span4 {
				margin-left: 10px;
				padding-left: 19px;
				border-left: 1px solid #eee;
			}
			#page-loading{
				float: right;
				margin-top: 8px;
			}
			.brand {
				margin-bottom: -12px;
				height: 16px;
				width: 169px;
				background: url({{ asset('bundles/plantnetdata/images/plantnet.png') }}) no-repeat;
			}
			.brand:hover {
				margin-bottom: -12px;
				height: 16px;
				width: 169px;
				background: url({{ asset('bundles/plantnetdata/images/plantnet.png') }}) no-repeat;
			}
		</style>
	{% endblock %}
</head>
<body>
	{% block topbar %}
		<div id="menu" class="topbar" data-dropdown="dropdown">
			<div class="fill">
				<div class="container">
					<a class="brand" href="{{ path('_index') }}"></a>
					<ul class="nav">
						{% if current is not defined %}
							{% set current = '#' %}
						{% endif %}
						<li>
							<a {% if current == 'index' %} class="active" {% endif %} href="{{ url('_index') }}">
								{{ "tab-home"|trans }}
							</a>
						</li>
						{% if current != 'index' %}
							<li class="dropdown">
								<a class="dropdown-toggle" href="#">{{ "tab-coll"|trans}}</a>
								<ul class="dropdown-menu">
									{% render controller("PlantnetDataBundle:Frontend\\Data:menuCollectionList", {'project': project}) %}
								</ul>
							</li>
							<li>
								<a {% if current == 'search' %} class="active" {% endif %} href="{{ url('_search', {'project': project}) }}">
									Search
								</a>
							</li>
						{% endif %}
					</ul>
					{% if current != 'index' %}
					<form action="" class="pull-right">
						<input type="text" placeholder="{{ 'search-field'|trans }}">
					</form>
					{% endif %}
				</div>
			</div>
		</div>
	{% endblock %}
	<div class="container">
		<div class="content">
			<div class="page-header">
				{% block header %}{% endblock %}
			</div>
			<div class="row">
				<div class="span10">
					{% block breadcrumb %}{% endblock %}
					{% block main %}{% endblock %}
				</div>
				<div class="span4">
					{% block side %}{% endblock %}
				</div>
			</div>
		</div>
	<footer id="footer">
		{# {% block footer %}
		<ul>
		<div id="logoPlantnet">
		<li class="logofoot"><a href="http://www.plantnet-project.org" onclick="window.open(this.href);return false;"><img src="{{ asset('bundles/plantnetdata/images/footPlantnet.png') }}"  alt="Projet Plantnet"></a></li>

		<li class="logofoot"><a href="http://www.agropolis-fondation.fr/" onclick="window.open(this.href);return false;"><img src="{{ asset('bundles/plantnetdata/images/footAF.png') }}"  alt="Agropolis Fondation"></a></li>
		</div>


		</ul>

		<nav id="mention">
		<ul>
		<li class="liens"><a href="{{ url('_credits') }}">Credits</a></li>
		<img src="{{ asset('bundles/plantnetdata/images/favicon.ico') }}"  alt="Projet Plantnet">
		<li class="liens"><a href="{{ url('_mentions') }}">Mentions légales</a></li>
		<img src="{{ asset('bundles/plantnetdata/images/favicon.ico') }}"  alt="Projet Plantnet">
		<li class="liens"><a href="{{ url('_contacts') }}">Contacts</a></li>
		<img src="{{ asset('bundles/plantnetdata/images/favicon.ico') }}"  alt="Projet Plantnet">
		<li class="liens"><a href="#">Plan du site</a></li>
		</ul>
		</nav>
		{% endblock %}#}
	</footer>
	{% block javascripts %}
		{% javascripts
			'bundles/plantnetdata/js/jquery-1.7.min.js'
			'bundles/plantnetdata/js/jquery.preloader.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-alerts.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-buttons.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-dropdown.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-modal.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-twipsy.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-popover.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-scrollspy.js'
			'bundles/plantnetdata/js/bootstrap/bootstrap-tabs.js'
			'bundles/plantnetdata/js/spin.min.js'
			filter='?yui_js' output="js/publish.js" debug=false %}
			<script src="{{ asset_url }}"></script>
		{% endjavascripts %}
		<script type="text/javascript" src="{{ asset('bundles/fosjsrouting/js/router.js') }}"></script>
		<script type="text/javascript" src="{{ path('fos_js_routing_js', {"callback": "fos.Router.setData"}) }}"></script>
		<script type="text/javascript" src="{{ asset('bundles/bazingaexposetranslation/js/translation.js') }}"></script>
		<script type="text/javascript" src="{{ url('bazinga_exposetranslation_js') }}"></script>
		{#<script type="text/javascript">
		(function () {
		if (/Microsoft/.test(navigator.appName)) { return }

		window.onload = function () {
		var menu = document.getElementById('menu');
		var init = menu.offsetTop;
		var docked;

		window.onscroll = function () {
		if (!docked && (menu.offsetTop - scrollTop() < 0)) {
		menu.style.top = 0;
		menu.style.position = 'fixed';
		menu.className = 'docked';
		docked = true;
		} else if (docked && scrollTop() <= init) {
		menu.style.position = 'absolute';
		menu.style.top = init + 'px';
		menu.className = menu.className.replace('docked', 'topbar');
		docked = false;
		}
		};


		(function () {
		var link     = document.getElementById('guide-link'),
		menu     = document.getElementById('menu'),
		dropdown = document.getElementById('dropdown');



		})();
		};

		function scrollTop() {
		return document.body.scrollTop || document.documentElement.scrollTop;
		}
		})();
		</script>#}
		<script type="text/javascript">
			$.ajaxSetup ({
				cache: false
			});
			{#$(".span10 li").on("click","a", function(event){
			event.preventDefault();
			var $url = $(this).attr('href');

			ajaxLoader();

			$(".span10").load($url);

			});#}



			{#$("#load_basic").click(function(){
			$(".span10").hide().load($url).fadeIn('slow');
			});#}
			function ajaxLoader(){
				var opts = {
					lines: 10, // The number of lines to draw
					length: 2, // The length of each line
					width: 3, // The line thickness
					radius: 7, // The radius of the inner circle
					color: '#2B5261', // #rgb or #rrggbb
					speed: 1.2, // Rounds per second
					trail: 50, // Afterglow percentage
					shadow: false // Whether to render a shadow
				};
				var target = document.getElementById("page-loading");
				var spinner = new Spinner(opts).spin(target);
			}
			$(".row .span10").preloader();
		</script>
	{% endblock %}
</body>
</html>